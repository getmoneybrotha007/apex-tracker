[phases.setup]
nixPkgs = ["python311Full"]

[phases.install]
cmds = ["python3.11 -m pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "python3.11 -m gunicorn server:app --bind 0.0.0.0:$PORT"
